<h1><%= current_user.name %> Task一覧</h1>

<% @tasks.each do |task| %>
  <ul>
    <li><%= link_to task.id, task %> : <%= task.content %> / <%= task.status %></li>
  </ul>
<% end %>

<%= link_to '新規Task登録ページ', new_task_path %><br>
<%= link_to 'ログアウト', logout_path, method: :delete %>