<h1>id:<%= @task.id %>Taskの詳細ページ</h1>

<div>
  <p><%= @task.id %> : <%= @task.content %> / <%= @task.status %></p>
  <p>
    <%= link_to 'このTaskを編集する', edit_task_path(@task) %>
    <%= link_to '削除', task_path(@task), method: :delete, data: { confirm: '本当に削除してもよろしいですか？' } %>
  </p>
</div>

<%= link_to 'Task一覧ページ', root_path %>

